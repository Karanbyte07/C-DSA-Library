#include <iostream>
#include <math.h>
using namespace std;
/*
// Function to count the number of set bits in a number
int countSetBits(int n) {
    int count = 0;
    while (n) {
        count += n & 1; // Increment count if the last bit is 1
        n >>= 1;        // Right shift n by 1 bit
    }
    return count;
}

int main() {
  int num1, num2 ;
  
  // Input two numbers from the user
  cout << "Enter num1: ";
  cin >> num1;

  cout << "Enter num1: ";
  cin >> num2;


  // Count the number of set bits in both numbers
  int setBitsNum1 = countSetBits(num1);
  int setBitsNum2 = countSetBits(num2);
  
  // Calculate the total number of set bits
  int totalSetBits = setBitsNum1 + setBitsNum2;
  
 
 // int answer = countSetBits();
  cout << "Total number of set bits in " << num1 << " and " << num2 << " is: " << totalSetBits << endl;


  
}
*/